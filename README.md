Create a file in this folder `.env.local`

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

Populate this file with your URL and Key.

### 5. Run the application

Run the application: `npm run dev`. Open your browser to the url indicated in the CLI (eg
`https://localhost:5173/`) and you are ready to go 🚀.
